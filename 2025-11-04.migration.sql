-- Add the fields to support a new member registering - name, email etc.
ALTER TABLE membership_sales
ADD COLUMN
IF NOT EXISTS 
ms_usr1_title varchar
(50),
ADD COLUMN
IF NOT EXISTS 
ms_usr2_title varchar
(50);